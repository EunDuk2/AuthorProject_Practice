#???? ?? ?? ??
#spring.application.name=basic

#?? ?? ???? 8080???
#server.port=8080


server:
  port: 8080

spring:
  config:
    activate:
      on-profile: local
  # ??? jdbc? ?? db ?? ????, jdbc ?? ??? mybatis / jpa ? spring? ???? DB ??? ???? ??
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/board
    username: root
    password: 1234
  jpa:
    database: mysql
    # innoDB? ??? ?? ??
    database-platform: org.hibernate.dialect.MariaDBDialect
    # ddl ? ???? ?? true. ???? ????(??)???, ???? ??? ??? ?? ??
    generate-ddl: true
    # jpa(?????)? ???? ???? hibernate ??.
    hibernate:
      # create : ?? table? drop? ? ??
      # update : entity ????? alter? ???? ?? ??? ??
      # validate : ?? DB?? ???? check? ?? ddl ? ?? x, ???? ???? ??.
      ddl-auto: update
    # ???? ddl, dml ?? ??? ???? ???? ??
    show_sql: true

logging:
  level:
    # debug, info, error ?? ??
    #    root: debug
    root: info # default

# mapper?? (mybatis? ????)? ??? ???? scanning??. ????? resource??? default.
mybatis:
  mapper-locations: classpath:/mapper/**/*.xml